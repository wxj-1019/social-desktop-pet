{
  "name": "@pet/supabase",
  "version": "0.0.0",
  "private": true,
  "description": "Supabase 后端：DB migrations（9.9 schema + RLS + pgvector）+ Edge Functions 骨架",
  "scripts": {
    "typecheck": "deno check --config deno.json functions/chat/index.ts functions/sync/index.ts 2>/dev/null || echo '本地未装 deno，类型检查由 CI 执行（deno check）'",
    "lint": "deno lint --config deno.json functions/ 2>/dev/null || echo '本地未装 deno，lint 由 CI 执行（deno lint）'",
    "db:reset": "supabase db reset",
    "functions:serve": "supabase functions serve --env-file .env.local",
    "start": "supabase start"
  }
}
