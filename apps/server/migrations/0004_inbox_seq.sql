-- ============================================================================
-- 0004_inbox_seq.sql —— 全局收件箱序列（D-13 自建后端，2026-08-01）
-- ============================================================================
-- 9.6：inbox_seq 保证单用户的补偿顺序（分页与游标）。
-- 实现：全局序列 nextval。全局单调 → 每个用户拿到的值严格递增；
-- 不同用户之间不连续（可接受），且与 PK (user_id, inbox_seq) 天然无冲突。
-- ============================================================================

create sequence if not exists user_inbox_seq;
