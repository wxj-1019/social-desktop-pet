-- ============================================================================
-- 0005_visits_bond_nullable.sql —— 拜访先于羁绊（D-13 实现细节，2026-08-01）
-- ============================================================================
-- 0001 中 visits.bond_id 为 not null，但 MVP 流程里拜访（6.6 wave/snack/留言）
-- 在首次见面/羁绊逻辑（6.4/6.5）接入前即可发生。先放宽约束；
-- 羁绊任务接入后由 findOrCreateBond 回填 bond_id 并恢复 not null。
-- ============================================================================

alter table visits alter column bond_id drop not null;
